---
interface Props {
  author: string;
  url: string;
}

const { author, url } = Astro.props;
---

<span>
  photo by <a
    class="hover:text-green underline transition-all duration-150 ease-in-out"
    href={url}>{author}</a
  >
</span>
